{{- $releaseName := .Release.Name -}}
{{- $chartName := include "infra-sanity-ervice.name" . -}}
{{- $cloudProviderFlavor := include "infra-sanity-ervice.cloudProviderFlavor" . -}}
{{- if .Values.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-service" (include "infra-sanity-ervice.fullname" .) }}
  labels:
    app: {{ $chartName }}
    component: {{ $chartName }}
    environment: {{ include "infra-sanity-ervice.environment" . }}
    release: {{ $releaseName }}
    {{- include "infra-sanity-ervice.labels" . | nindent 4 }}
spec:
  {{- if eq $cloudProviderFlavor "minikube" }}
  type: NodePort
  {{- end }}
  ports:
    - port: {{ .Values.env.port }}
      targetPort: {{ .Values.env.targetPort }}
      protocol: {{ .Values.env.protocol }}
      name: http
      {{- if eq $cloudProviderFlavor "minikube" }}
      nodePort: {{ .Values.nodePort }}
      {{- end }}
  selector:
    app: {{ $chartName }}
    release: {{ $releaseName }}
    run: {{ $releaseName }}-{{ $chartName }}
    {{- include "infra-sanity-ervice.selectorLabels" . | nindent 4 }}
{{- end }}
