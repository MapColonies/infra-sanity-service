{{- if and .Values.serviceAccount.create .Values.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
  labels: {{- include "checkmate.labels" . | nindent 4 }}
{{- end }}
